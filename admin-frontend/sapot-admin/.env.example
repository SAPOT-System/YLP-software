API_DOMAIN=https://192.168.254.106:8000
NEXT_PUBLIC_MAP_STYLE=http://192.168.254.106:8080/styles/basic-preview/style.json
NEXT_PUBLIC_WEBSOCKET_DOMAIN=wss://192.168.254.106
NODE_ENV=development
# Path to the CA certificate that signed the server's TLS certificate. Place the
# .pem file at this path so Node trusts renewed server certificates without disabling TLS verification globally.
NODE_EXTRA_CA_CERTS=./certs/server_ca.pem

# Comma-separated list of LAN IPs/hostnames allowed to reach the Next.js dev server
# (silences the cross-origin dev warning). Set to your own machine's LAN IP.
DEV_ORIGINS=192.168.0.99,192.168.0.100,192.168.1.23

