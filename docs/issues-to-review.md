# Issues to Review — Validation Status

Legend: ✅ confirmed bug · 🟡 partially confirmed / partially implemented / needs repro · 🔵 feature or docs request — valid (absent, or behaviour-exists change-request) · ⚪ not reproduced / already handled / already exists

- [ ] ✅ **1.** When in the call room, the user can back and close the call — *no `BackHandler` in the call room; hardware back navigates away without ending the call, leaving a dangling session.*
- [x] ⚪ **2.** End call button is missing when calling peers — *End Call button renders during the `calling` state and is deliberately pinned (see `call/[id].tsx:149-159`). Appears already fixed.*
- [ ] ✅ **3.** There is no server database migration — *server uses `SQLModel.metadata.create_all()` only (no Alembic); cannot alter existing tables.*
- [ ] ✅ **4.** Messages in the chat room are not in sequence by the time they are sent — *receiver drops the wire `sentAt`; `createdAt` = local receive time, list orders by `created_at`.*
- [ ] 🟡 **5.** Online icon is not appearing in LAN mode — *LAN path is wired end-to-end (mDNS `markOnline` → `peers.is_online` → peer-list). Likely a runtime mDNS/Zeroconf issue; needs on-device repro.*
- [x] ⚪ **6.** There is no auto capitalize in the LAN login — *both LAN login fields already set `autoCapitalize="words"` (`lan-login.tsx:107,118`).*
- [ ] 🟡 **7.** There is missing error handling in calls — *broad; try/catch is extensive. Minor gaps only (e.g. fire-and-forget `startCall` in `use-call-lifecycle.ts:61`). Low confidence.*
- [ ] 🔵 **8.** Should remove scroll requirement in terms and conditions — *scroll gate exists (`terms-modal.tsx:177`, `hasScrolledToBottom`). Valid UX change request.*
- [ ] ✅ **9.** Long messages cannot be handled by server and mobile app — *server caps `content` at 255 chars; composer `TextInput` has no `maxLength` and is not `multiline`.*
- [ ] ✅ **10.** When reconnecting, the call room cannot be interrupted (buttons unclickable) — *5s auto-hide during `reconnecting` + full-screen overlay/video views swallow the tap-to-reveal (`call/[id].tsx:273-277`, no `pointerEvents="none"`).*
- [ ] 🟡 **11.** Guest login bug when server unavailable — *guest login path (`syncGuestUser`→`initialize({isGuest:true})`) is fully offline-safe. Root cause not reproduced statically; needs repro (check MainContainer/WS-encryption startup).*
- [ ] ✅ **12.** Syncing issue in Sapot admin — *`pull()` advances the global cursor to `timestamp` on each page before pagination completes → interrupted multi-page pulls skip data; `push()` clears the whole queue after async work → mutations enqueued during push are lost.*
- [ ] 🟡 **13.** Encryption problem in admin — *key derivation matches mobile, but admin caches only one latest key per peer (no historical candidates), and silently falls back to plaintext/raw ciphertext.*
- [ ] 🔵 **14.** Should remove the PIN — *PIN feature exists (`pin-entry-gate.tsx`, `settings/account/encryption-pin.tsx`). Valid product change request.*
- [ ] 🔵 **15.** Should remove the linking_message_id — *field exists as `linked_message_id` (server `Message`) / `linkedMessageId` (mobile), used for SMS↔P2P linking. Valid change request.*
- [x] ⚪ **16.** There is no public chat status — *public-chat screen already shows a Connected/Connecting status bar (`public-chat.tsx:68-79`). (If "presence/member count" was meant, that is a separate feature.)*
- [ ] ✅ **17.** Messages disappear if there are a lot; no mechanism to handle — *message list queries `sortBy(created_at, asc) + take(100)` → shows the OLDEST 100, no pagination; newer messages beyond 100 are excluded.*
- [ ] ✅ **18.** Message dates are not accurate on what day they were sent — *same root cause as #4: receiver drops `sentAt`; server sets `created_at` at insert time.*
- [ ] ✅ **19.** The location permission is not asked — *`gps-location-service.watchPositionAsync` requests no permission; only `map.tsx` (via `useLocationPermission`) does, so the rescuer streaming path can start without asking.*
- [ ] ✅ **20.** Rejecting a call → peer receives "call ended" instead of "call rejected" — *`call-message-router` treats a declined `call-rejected` as `noop`; `terminateCallConnection` also emits `call-ended`, which drives the UI.*
- [ ] ✅ **21.** Message count not resetting when chatting with admin — *`markConversationAsRead` is gated on `isConnected` (`chat/[id].tsx:427`); server-delivered admin messages (no live P2P link) never clear the badge.*
## Backlog items (22+) — reviewed

These are feature / infra / docs requests rather than bug reports. Verdicts reflect whether the thing already exists in the code/docs, not whether a bug reproduces.

- [ ] 🔵 **22.** Implementation of TURN server — *absent. `webrtc-adapter.ts:62` sets `iceServers: []` (no STUN/TURN anywhere). Calls rely on host/LAN candidates only; cross-NAT/relay traversal is unimplemented. Valid (aligns with LAN-first design; needed for internet-relayed calls).*
- [ ] 🟡 **23.** Offline mobile app updates with server and expo — *partial. `expo-updates` installed and EAS channels wired (`package.json` `update:dev/prev/prod`, `app.config.ts` `runtimeVersion`/`getChannel`). But OTA fetches from EAS servers — no self-hosted/offline update delivery for a disconnected LAN deployment. Delivery-offline path not implemented.*
- [ ] 🟡 **24.** Offline sentry — *partial. `@sentry/react-native` initialized (`app/_layout.tsx:137`) with a hardcoded **cloud** DSN (`…ingest.de.sentry.io`); RN SDK buffers offline but cannot deliver in a disconnected LAN. Server has `sentry-sdk` in requirements but no `sentry_sdk.init` found in `main.py`. Self-hosted/offline sink not wired.*
- [ ] 🔵 **25.** Rate limiting for TCP server mobile app — *absent. `tcp-server-adapter.ts` has no connection/message rate limiting (only unrelated `MAX_CONNECT_RETRIES` on the client planner). Valid hardening request (was on a security branch, not on develop).*
- [ ] 🟡 **26.** Restrict two devices in one user — *partial. Device infra exists (`models/devices.py`, `device_key.py`, `db_operations/device_attempts.py`) but no single-active-device enforcement found — multiple device registrations are allowed. Restriction itself is unimplemented.*
- [ ] 🟡 **27.** Mobile app syncing issues — *needs specifics. `features/sync/services/sync-service.ts` exists; symptom is unspecified. Most likely overlaps confirmed **#4/#17** (timestamp/ordering + oldest-100 window). Reproduce a concrete symptom or fold into those.*
- [ ] 🟡 **28.** Unrestrict user in attempts punishment through admin — *partial. Admin `/ban/user` + `/unban/user` exist (`api/admin.py:667,706`), and IP/login lockout exists (`login_attempt.py`, `device_attempts.py`, `test_ip_lockout.py`). But no admin endpoint to clear an **attempt-based lockout** was found — ban/unban ≠ lockout reset. Gap is the lockout-clear action.*
- [ ] 🔵 **29.** Communication with admin if user is banned — *absent. Ban system exists (`banned_user.py`) but there is no channel for a banned user to reach an admin. Valid feature request.*
- [ ] 🔵 **30.** SOS feature implementation — *absent. Zero references to SOS/emergency/panic/distress in mobile or server. Fully unimplemented.*
- [ ] 🔵 **31.** Implementation of grafana for logging — *absent. `docs/deployment/monitoring-logging.md` states "No external monitoring agent is required" and TODOs Prometheus; no Grafana/Prometheus/Loki in repo. Valid infra request.*
- [ ] ⚪ **32.** Implementation of getting started screens — *already exists. `features/getting-started/` and `app/getting-started/` are present in the mobile app. (Confirm scope/coverage, but screens exist.)*
- [ ] 🟡 **33.** Implementation of new user guide — *partial / overlaps #32. In-app onboarding lives under `getting-started`; a distinct post-onboarding "user guide" is not clearly separate. Clarify whether this differs from #32.*
- [ ] 🔵 **34.** MikroTik Configuration Steps docs — *doc gap confirmed. `docs/architecture/networking-lan-model.md:72` has an explicit "TODO (human input required): Document MikroTik configuration steps (SSID, DHCP, captive portal, RouterOS API creds, static IP)." Stub exists, content missing.*
- [ ] 🟡 **35.** Disaster-scenario operating constraints docs — *partial. Context exists in `adr/0005-lan-first-design.md` and `deployment/runbooks.md` (disaster recovery), but no dedicated "operating constraints" doc. Consolidate into one.*
- [ ] 🟡 **36.** SERIAL_PORT in GSM module identification guide docs — *partial. `SERIAL_PORT`/`/dev/ttyACM0` is documented as config (`deployment/gsm-module.md:11,39`, `getting-started/gsm-module-setup.md:8,30`), but there is no guide on **how to identify** the correct port (e.g. `ls /dev/ttyACM*`, `dmesg`). Identification steps missing.*
- [ ] 🔵 **37.** Uptime monitor in monitoring-logging — *doc/infra gap confirmed. `docs/deployment/monitoring-logging.md:73` explicitly TODOs whether an uptime monitor (UptimeRobot/Prometheus/cron ping) is in use. Not implemented/documented.*
- [ ] 🟡 **38.** pnpm and uv conflicting with the previous tooling — *confirmed inconsistency, low severity. Repo standard is **npm** (`package-lock.json`, mobile app) but admin-frontend docs prescribe **pnpm** (`deployment/admin-frontend.md`, `overview.md:27`). **uv** is not actually used (server uses `requirements.txt`, no `uv.lock`/`pyproject`). Align tooling docs, drop the uv reference or adopt it explicitly.*
