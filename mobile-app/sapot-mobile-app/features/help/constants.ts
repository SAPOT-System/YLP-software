/** Bump only for a materially changed tour that should be replayed. */
export const CURRENT_TOUR_VERSION = 1;

/** Time to wait for a focused screen to lay out a tour target. */
export const ANCHOR_WAIT_MS = 500;
